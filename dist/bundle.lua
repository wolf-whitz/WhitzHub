local __mod0 = (function() local Intro = {} function Intro.createIntro(callback) local Players = game:GetService("Players") local TweenService = game:GetService("TweenService") local RunService = game:GetService("RunService") local player = Players.LocalPlayer if not player then if callback then callback() end return end local playerGui = player:WaitForChild("PlayerGui") local gui = Instance.new("ScreenGui") gui.Name = "WhitzHubIntro" gui.IgnoreGuiInset = true gui.DisplayOrder = 100 gui.Parent = playerGui local bg = Instance.new("Frame") bg.Size = UDim2.new(1, 0, 1, 0) bg.BackgroundColor3 = Color3.fromRGB(0, 0, 0) bg.BackgroundTransparency = 1 bg.Parent = gui local gradient = Instance.new("UIGradient", bg) gradient.Rotation = 90 gradient.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(10, 10, 20)), ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0)) }) local blur = Instance.new("BlurEffect") blur.Size = 0 blur.Parent = game:GetService("Lighting") local title = Instance.new("TextLabel") title.Text = "WhitzHub" title.Size = UDim2.new(0, 400, 0, 80) title.Position = UDim2.new(0.5, 0, 0.45, 0) title.AnchorPoint = Vector2.new(0.5, 0.5) title.TextColor3 = Color3.fromRGB(255, 255, 255) title.TextScaled = true title.Font = Enum.Font.GothamBlack title.BackgroundTransparency = 1 title.TextTransparency = 1 title.Parent = gui local subtitle = Instance.new("TextLabel") subtitle.Text = "created by whitzscott" subtitle.Size = UDim2.new(0, 400, 0, 35) subtitle.Position = UDim2.new(0.5, 0, 0.56, 0) subtitle.AnchorPoint = Vector2.new(0.5, 0.5) subtitle.TextColor3 = Color3.fromRGB(180, 180, 180) subtitle.TextScaled = true subtitle.Font = Enum.Font.Gotham subtitle.BackgroundTransparency = 1 subtitle.TextTransparency = 1 subtitle.Parent = gui local glow = Instance.new("UIStroke", title) glow.Thickness = 1.6 glow.Transparency = 1 glow.Color = Color3.fromRGB(100, 100, 255) local tweenIn = TweenInfo.new(1, Enum.EasingStyle.Quint, Enum.EasingDirection.Out) local tweenOut = TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.In) local bgFadeIn = TweenService:Create(bg, tweenIn, {BackgroundTransparency = 0.2}) local blurIn = TweenService:Create(blur, tweenIn, {Size = 12}) local titleIn = TweenService:Create(title, tweenIn, {TextTransparency = 0, Position = UDim2.new(0.5, 0, 0.43, 0)}) local subtitleIn = TweenService:Create(subtitle, TweenInfo.new(1.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {TextTransparency = 0}) local glowIn = TweenService:Create(glow, TweenInfo.new(1, Enum.EasingStyle.Quad), {Transparency = 0.5}) local glowPulse = TweenService:Create(glow, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {Transparency = 0.2}) local fadeOut = TweenService:Create(bg, tweenOut, {BackgroundTransparency = 1}) local blurOut = TweenService:Create(blur, tweenOut, {Size = 0}) local titleOut = TweenService:Create(title, tweenOut, {TextTransparency = 1, Position = UDim2.new(0.5, 0, 0.4, 0)}) local subtitleOut = TweenService:Create(subtitle, tweenOut, {TextTransparency = 1, Position = UDim2.new(0.5, 0, 0.6, 0)}) bgFadeIn:Play() blurIn:Play() titleIn:Play() subtitleIn:Play() glowIn:Play() task.wait(0.8) glowPulse:Play() task.wait(2.5) fadeOut:Play() blurOut:Play() titleOut:Play() subtitleOut:Play() titleOut.Completed:Wait() glowPulse:Cancel() blur:Destroy() gui:Destroy() if callback and type(callback) == "function" then task.spawn(callback) end end return Intro  end)() local __mod1 = (function() local Setting = {} local TweenService = game:GetService("TweenService") local UserInputService = game:GetService("UserInputService") local HttpService = game:GetService("HttpService") local SETTINGS_FILE = "settings.json" local function readSettings() local data pcall(function() if isfile and isfile(SETTINGS_FILE) then data = HttpService:JSONDecode(readfile(SETTINGS_FILE)) end end) return data or { animations = true, darkmode = true, tooltips = true } end local function saveSettings(data) pcall(function() if writefile then writefile(SETTINGS_FILE, HttpService:JSONEncode(data)) end end) end function Setting.open(gui) if gui:FindFirstChild("SettingWindow") then return end local settings = readSettings() local blur = Instance.new("Frame") blur.Size = UDim2.new(1, 0, 1, 0) blur.BackgroundColor3 = Color3.fromRGB(0, 0, 0) blur.BackgroundTransparency = 1 blur.ZIndex = 8 blur.Parent = gui local window = Instance.new("Frame") window.Name = "SettingWindow" window.Size = UDim2.new(0, 420, 0, 320) window.Position = UDim2.new(0.5, 0, 0.5, 0) window.AnchorPoint = Vector2.new(0.5, 0.5) window.BackgroundColor3 = Color3.fromRGB(25, 25, 25) window.BackgroundTransparency = 0.2 window.ZIndex = 9 window.ClipsDescendants = true window.Parent = gui local corner = Instance.new("UICorner", window) corner.CornerRadius = UDim.new(0, 10) local stroke = Instance.new("UIStroke", window) stroke.Color = Color3.fromRGB(255, 255, 255) stroke.Thickness = 1.5 stroke.Transparency = 0 local title = Instance.new("TextLabel") title.Size = UDim2.new(1, -40, 0, 40) title.Position = UDim2.new(0, 20, 0, 0) title.BackgroundTransparency = 1 title.Text = "Settings" title.Font = Enum.Font.GothamBold title.TextSize = 22 title.TextColor3 = Color3.fromRGB(240, 240, 240) title.TextXAlignment = Enum.TextXAlignment.Left title.ZIndex = 10 title.Parent = window local close = Instance.new("TextButton") close.Size = UDim2.new(0, 26, 0, 26) close.Position = UDim2.new(1, -32, 0, 8) close.BackgroundColor3 = Color3.fromRGB(50, 50, 50) close.BackgroundTransparency = 0.4 close.Text = "✕" close.Font = Enum.Font.GothamBold close.TextSize = 16 close.TextColor3 = Color3.fromRGB(240, 240, 240) close.ZIndex = 10 close.Parent = window local closeCorner = Instance.new("UICorner", close) closeCorner.CornerRadius = UDim.new(0, 6) local body = Instance.new("Frame") body.Size = UDim2.new(1, -40, 1, -60) body.Position = UDim2.new(0, 20, 0, 50) body.BackgroundTransparency = 1 body.ZIndex = 9 body.Parent = window local layout = Instance.new("UIListLayout", body) layout.Padding = UDim.new(0, 8) layout.SortOrder = Enum.SortOrder.LayoutOrder local function createToggle(text, key) local toggle = Instance.new("TextButton") toggle.Size = UDim2.new(1, 0, 0, 36) toggle.BackgroundColor3 = settings[key] and Color3.fromRGB(80, 120, 255) or Color3.fromRGB(40, 40, 40) toggle.TextColor3 = Color3.fromRGB(240, 240, 240) toggle.Text = text .. ": " .. (settings[key] and "ON" or "OFF") toggle.Font = Enum.Font.Gotham toggle.TextSize = 16 toggle.AutoButtonColor = false toggle.ZIndex = 9 toggle.Parent = body local corner = Instance.new("UICorner", toggle) corner.CornerRadius = UDim.new(0, 8) local stroke = Instance.new("UIStroke", toggle) stroke.Color = Color3.fromRGB(255, 255, 255) stroke.Transparency = 0 toggle.MouseEnter:Connect(function() TweenService:Create(toggle, TweenInfo.new(0.2), {BackgroundColor3 = settings[key] and Color3.fromRGB(100, 140, 255) or Color3.fromRGB(60, 60, 60)}):Play() end) toggle.MouseLeave:Connect(function() TweenService:Create(toggle, TweenInfo.new(0.2), {BackgroundColor3 = settings[key] and Color3.fromRGB(80, 120, 255) or Color3.fromRGB(40, 40, 40)}):Play() end) toggle.MouseButton1Click:Connect(function() settings[key] = not settings[key] saveSettings(settings) toggle.Text = text .. ": " .. (settings[key] and "ON" or "OFF") TweenService:Create(toggle, TweenInfo.new(0.25), {BackgroundColor3 = settings[key] and Color3.fromRGB(80, 120, 255) or Color3.fromRGB(40, 40, 40)}):Play() end) end createToggle("Enable animations", "animations") createToggle("Dark mode", "darkmode") createToggle("Show tooltips", "tooltips") local reset = Instance.new("TextButton") reset.Size = UDim2.new(1, 0, 0, 36) reset.BackgroundColor3 = Color3.fromRGB(60, 40, 40) reset.TextColor3 = Color3.fromRGB(240, 240, 240) reset.Text = "Reset settings" reset.Font = Enum.Font.GothamBold reset.TextSize = 16 reset.AutoButtonColor = false reset.ZIndex = 9 reset.Parent = body local resetCorner = Instance.new("UICorner", reset) resetCorner.CornerRadius = UDim.new(0, 8) local resetStroke = Instance.new("UIStroke", reset) resetStroke.Color = Color3.fromRGB(255, 255, 255) resetStroke.Transparency = 0 reset.MouseButton1Click:Connect(function() settings = {animations = true, darkmode = true, tooltips = true} saveSettings(settings) window:Destroy() blur:Destroy() Setting.open(gui) end) window.BackgroundTransparency = 1 window.Size = UDim2.new(0, 0, 0, 0) TweenService:Create(blur, TweenInfo.new(0.25), {BackgroundTransparency = 0.5}):Play() TweenService:Create(window, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { BackgroundTransparency = 0.2, Size = UDim2.new(0, 420, 0, 320) }):Play() close.MouseButton1Click:Connect(function() TweenService:Create(blur, TweenInfo.new(0.25), {BackgroundTransparency = 1}):Play() local tween = TweenService:Create( window, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), BackgroundTransparency = 1} ) tween:Play() tween.Completed:Connect(function() window:Destroy() blur:Destroy() end) end) local dragging, dragInput, mousePos, framePos title.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true mousePos = input.Position framePos = window.Position input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end) end end) title.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end end) UserInputService.InputChanged:Connect(function(input) if input == dragInput and dragging then local delta = input.Position - mousePos window.Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y) end end) end return Setting  end)() local __mod2 = (function() local WindowControls = {} local UserInputService = game:GetService("UserInputService") local TweenService = game:GetService("TweenService") local HttpService = game:GetService("HttpService") local SETTINGS_FILE = "settings.json" local function readSettings() local data pcall(function() if isfile and isfile(SETTINGS_FILE) then data = HttpService:JSONDecode(readfile(SETTINGS_FILE)) end end) return data or { darkmode = true } end function WindowControls.create(topBar, window, gui) local settings = readSettings() local dark = settings.darkmode local colors = dark and { bg = Color3.fromRGB(35, 35, 35), hover = Color3.fromRGB(70, 70, 90), icon = Color3.fromRGB(255, 255, 255), hoverIcon = Color3.fromRGB(180, 200, 255) } or { bg = Color3.fromRGB(230, 230, 230), hover = Color3.fromRGB(210, 210, 255), icon = Color3.fromRGB(30, 30, 30), hoverIcon = Color3.fromRGB(70, 100, 255) } local function styleButton(btn) btn.BackgroundColor3 = colors.bg btn.BackgroundTransparency = 0.3 btn.ImageColor3 = colors.icon btn.AutoButtonColor = false btn.BorderSizePixel = 0 btn.ZIndex = 2 local corner = Instance.new("UICorner", btn) corner.CornerRadius = UDim.new(0, 6) btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), { BackgroundTransparency = 0.1, ImageColor3 = colors.hoverIcon, BackgroundColor3 = colors.hover }):Play() end) btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), { BackgroundTransparency = 0.3, ImageColor3 = colors.icon, BackgroundColor3 = colors.bg }):Play() end) end local settingsButton = Instance.new("ImageButton") settingsButton.Size = UDim2.new(0, 30, 0, 30) settingsButton.Position = UDim2.new(0, 8, 0, 2) settingsButton.Image = "rbxassetid://102254373423014" settingsButton.ImageRectOffset = Vector2.new(964, 324) settingsButton.ImageRectSize = Vector2.new(36, 36) styleButton(settingsButton) settingsButton.Parent = topBar local closeButton = Instance.new("ImageButton") closeButton.Size = UDim2.new(0, 30, 0, 30) closeButton.Position = UDim2.new(1, -35, 0, 2) closeButton.Image = "rbxassetid://116533143721125" styleButton(closeButton) closeButton.Parent = topBar local minimizeButton = Instance.new("ImageButton") minimizeButton.Size = UDim2.new(0, 30, 0, 30) minimizeButton.Position = UDim2.new(1, -70, 0, 2) minimizeButton.Image = "rbxassetid://10137941941" styleButton(minimizeButton) minimizeButton.Parent = topBar local maximizeButton = Instance.new("ImageButton") maximizeButton.Size = UDim2.new(0, 30, 0, 30) maximizeButton.Position = UDim2.new(1, -105, 0, 2) maximizeButton.Image = "rbxassetid://11036884234" styleButton(maximizeButton) maximizeButton.Parent = topBar local isMinimized, isMaximized = false, false local originalSize = window.Size local originalPos = window.Position local contentFrame = Instance.new("Frame") contentFrame.Name = "ContentFrame" contentFrame.Size = UDim2.new(1, 0, 1, -35) contentFrame.Position = UDim2.new(0, 0, 0, 35) contentFrame.BackgroundTransparency = 1 contentFrame.ClipsDescendants = true contentFrame.Parent = window local function updateContentFrame() local topBarHeight = topBar.AbsoluteSize.Y contentFrame.Size = UDim2.new(1, 0, 1, -topBarHeight) contentFrame.Position = UDim2.new(0, 0, 0, topBarHeight) end window:GetPropertyChangedSignal("Size"):Connect(updateContentFrame) window:GetPropertyChangedSignal("Position"):Connect(updateContentFrame) local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out) settingsButton.MouseButton1Click:Connect(function() local ok, Setting = pcall(function() return __mod1 end) if ok and Setting and Setting.open then Setting.open(gui) end end) minimizeButton.MouseButton1Click:Connect(function() isMinimized = not isMinimized local goal = isMinimized and UDim2.new(originalSize.X.Scale, originalSize.X.Offset, 0, 35) or originalSize TweenService:Create(window, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Size = goal }):Play() end) maximizeButton.MouseButton1Click:Connect(function() isMaximized = not isMaximized local goal = isMaximized and { Size = UDim2.new(1, -20, 1, -20), Position = UDim2.new(0, 10, 0, 10) } or { Size = originalSize, Position = originalPos } TweenService:Create(window, TweenInfo.new(0.35, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), goal):Play() end) closeButton.MouseButton1Click:Connect(function() local flash = Instance.new("Frame") flash.Size = UDim2.new(1, 0, 1, 0) flash.BackgroundColor3 = Color3.fromRGB(255, 50, 50) flash.BackgroundTransparency = 0.7 flash.BorderSizePixel = 0 flash.ZIndex = 3 flash.Parent = window TweenService:Create(flash, TweenInfo.new(0.15), { BackgroundTransparency = 1 }):Play() local tween = TweenService:Create(window, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.In), { Position = window.Position + UDim2.new(0, 0, 0, 40), Size = UDim2.new(0, 0, 0, 0) }) tween:Play() tween.Completed:Connect(function() gui:Destroy() end) end) local dragging, dragInput, mousePos, framePos topBar.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true mousePos = input.Position framePos = window.Position input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end) end end) topBar.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end end) UserInputService.InputChanged:Connect(function(input) if input == dragInput and dragging then local delta = input.Position - mousePos window.Position = UDim2.new( framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y ) end end) updateContentFrame() return contentFrame end return WindowControls  end)() local __mod3 = (function() local TweenService = game:GetService("TweenService") local LeftPanel = {} local THEME = { Background = Color3.fromRGB(25, 25, 25), TabDefault = Color3.fromRGB(50, 50, 50), TabHover = Color3.fromRGB(75, 75, 75), TabActive = Color3.fromRGB(100, 100, 255), TextColor = Color3.fromRGB(240, 240, 240), Accent = Color3.fromRGB(0, 120, 255), } function LeftPanel.create(parent, onTabClick) local frame = Instance.new("Frame") frame.Name = "LeftPanel" frame.Size = UDim2.new(0, 160, 1, 0) frame.BackgroundColor3 = THEME.Background frame.BackgroundTransparency = 0.2 frame.Parent = parent Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10) local gradient = Instance.new("UIGradient", frame) gradient.Color = ColorSequence.new{ ColorSequenceKeypoint.new(0, Color3.fromRGB(35, 35, 35)), ColorSequenceKeypoint.new(1, Color3.fromRGB(15, 15, 15)) } gradient.Rotation = 90 local scroll = Instance.new("ScrollingFrame") scroll.Size = UDim2.new(1, 0, 1, 0) scroll.BackgroundTransparency = 1 scroll.ScrollBarThickness = 6 scroll.CanvasSize = UDim2.new(0, 0, 0, 0) scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y scroll.Parent = frame local layout = Instance.new("UIListLayout", scroll) layout.SortOrder = Enum.SortOrder.LayoutOrder layout.Padding = UDim.new(0, 6) local padding = Instance.new("UIPadding", scroll) padding.PaddingTop = UDim.new(0, 8) padding.PaddingLeft = UDim.new(0, 6) padding.PaddingRight = UDim.new(0, 6) local activeButton = nil local tabButtons = {} function LeftPanel.addTab(name, order) local btn = Instance.new("TextButton") btn.Name = name btn.Size = UDim2.new(1, -4, 0, 42) btn.Text = name btn.TextColor3 = THEME.TextColor btn.Font = Enum.Font.GothamMedium btn.TextSize = 16 btn.BackgroundColor3 = THEME.TabDefault btn.AutoButtonColor = false btn.LayoutOrder = order or 1 btn.Parent = scroll Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8) local accent = Instance.new("Frame") accent.Name = "Accent" accent.Size = UDim2.new(0, 0, 1, 0) accent.Position = UDim2.new(0, 0, 0, 0) accent.BackgroundColor3 = THEME.Accent accent.BorderSizePixel = 0 accent.ZIndex = 2 accent.Parent = btn tabButtons[name] = btn btn.MouseEnter:Connect(function() if btn ~= activeButton then TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = THEME.TabHover}):Play() end end) btn.MouseLeave:Connect(function() if btn ~= activeButton then TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = THEME.TabDefault}):Play() end end) btn.MouseButton1Click:Connect(function() if activeButton == btn then if onTabClick then onTabClick(name) end return end if activeButton then TweenService:Create(activeButton, TweenInfo.new(0.2), { BackgroundColor3 = THEME.TabDefault }):Play() local prevAccent = activeButton:FindFirstChild("Accent") if prevAccent then TweenService:Create(prevAccent, TweenInfo.new(0.25), {Size = UDim2.new(0, 0, 1, 0)}):Play() end end activeButton = btn TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = THEME.TabActive}):Play() TweenService:Create(accent, TweenInfo.new(0.25), {Size = UDim2.new(0, 4, 1, 0)}):Play() if onTabClick then onTabClick(name) end end) end function LeftPanel.switchTo(name) local btn = tabButtons[name] if btn then btn.MouseButton1Click:Fire() end end return LeftPanel, frame end return LeftPanel  end)() local __mod4 = (function() local RightPanel = {} local HttpService = game:GetService("HttpService") local TweenService = game:GetService("TweenService") local SETTINGS_FILE = "settings.json" local function readSettings() local data pcall(function() if isfile and isfile(SETTINGS_FILE) then data = HttpService:JSONDecode(readfile(SETTINGS_FILE)) end end) return data or { darkmode = true } end function RightPanel.create(parent) local settings = readSettings() local dark = settings.darkmode local TextColor = dark and Color3.fromRGB(240, 240, 240) or Color3.fromRGB(30, 30, 30) local BtnColor = dark and Color3.fromRGB(50, 50, 50) or Color3.fromRGB(240, 240, 240) local ExecuteColor = Color3.fromRGB(100, 100, 255) local ExecuteHover = Color3.fromRGB(150, 150, 255) local frame = Instance.new("Frame") frame.Name = "RightPanel" frame.Size = UDim2.new(1, -160, 1, 0) frame.Position = UDim2.new(0, 160, 0, 0) frame.BackgroundTransparency = 1 frame.Parent = parent local scroll = Instance.new("ScrollingFrame") scroll.Size = UDim2.new(1, 0, 1, 0) scroll.CanvasSize = UDim2.new(0, 0, 0, 0) scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y scroll.ScrollBarThickness = 6 scroll.BackgroundTransparency = 1 scroll.Parent = frame local layout = Instance.new("UIListLayout", scroll) layout.SortOrder = Enum.SortOrder.LayoutOrder layout.Padding = UDim.new(0, 6) local sectionButtons = {} local currentExpandedButton function RightPanel.addButton(section, data) if not sectionButtons[section] then sectionButtons[section] = {} end local btnFrame = Instance.new("TextButton") btnFrame.Size = UDim2.new(1, -10, 0, 42) btnFrame.BackgroundColor3 = BtnColor btnFrame.Text = "" btnFrame.AutoButtonColor = false btnFrame.Parent = scroll local corner = Instance.new("UICorner", btnFrame) corner.CornerRadius = UDim.new(0, 8) local stroke = Instance.new("UIStroke", btnFrame) stroke.Color = TextColor stroke.Transparency = 0.7 stroke.Thickness = 1 local nameLabel = Instance.new("TextLabel") nameLabel.Size = UDim2.new(1, -90, 0, 42) nameLabel.Position = UDim2.new(0, 5, 0, 0) nameLabel.BackgroundTransparency = 1 nameLabel.TextColor3 = TextColor nameLabel.Text = data.name nameLabel.Font = Enum.Font.GothamMedium nameLabel.TextSize = 15 nameLabel.TextXAlignment = Enum.TextXAlignment.Left nameLabel.Parent = btnFrame local executeBtn = Instance.new("TextButton") executeBtn.Size = UDim2.new(0, 70, 0, 30) executeBtn.Position = UDim2.new(1, -75, 0, 6) executeBtn.BackgroundColor3 = ExecuteColor executeBtn.TextColor3 = Color3.fromRGB(255, 255, 255) executeBtn.Text = "Execute" executeBtn.Font = Enum.Font.Gotham executeBtn.TextSize = 14 executeBtn.AutoButtonColor = false executeBtn.Parent = btnFrame local cornerEx = Instance.new("UICorner", executeBtn) cornerEx.CornerRadius = UDim.new(0, 6) local descLabel local expanded = false local dropdownHeight = 50 local function showDropdown() if descLabel then descLabel:Destroy() end descLabel = Instance.new("TextLabel") descLabel.Size = UDim2.new(1, -10, 0, dropdownHeight) descLabel.Position = UDim2.new(0, 5, 0, 42) descLabel.BackgroundTransparency = 1 descLabel.TextColor3 = TextColor descLabel.Text = data.description or "No description" descLabel.TextWrapped = true descLabel.TextXAlignment = Enum.TextXAlignment.Left descLabel.TextYAlignment = Enum.TextYAlignment.Top descLabel.Font = Enum.Font.Gotham descLabel.TextSize = 14 descLabel.Parent = btnFrame end local function hideDropdown() if descLabel then descLabel:Destroy() descLabel = nil end end btnFrame.MouseButton1Click:Connect(function() if currentExpandedButton and currentExpandedButton ~= btnFrame then TweenService:Create(currentExpandedButton, TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Size = UDim2.new(1, -10, 0, 42)}):Play() local otherDesc = currentExpandedButton:FindFirstChildWhichIsA("TextLabel") if otherDesc then otherDesc:Destroy() end end expanded = not expanded local goalSize = expanded and 42 + dropdownHeight or 42 TweenService:Create(btnFrame, TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Size = UDim2.new(1, -10, 0, goalSize)}):Play() if expanded then showDropdown() else hideDropdown() end currentExpandedButton = expanded and btnFrame or nil end) executeBtn.MouseEnter:Connect(function() TweenService:Create(executeBtn, TweenInfo.new(0.2), {BackgroundColor3 = ExecuteHover}):Play() end) executeBtn.MouseLeave:Connect(function() TweenService:Create(executeBtn, TweenInfo.new(0.2), {BackgroundColor3 = ExecuteColor}):Play() end) executeBtn.MouseButton1Click:Connect(function() if data.url and #data.url > 0 then local success, err = pcall(function() local func, loadErr = loadstring(game:HttpGet(data.url, true)) if not func then error(loadErr) end func() end) if not success then warn("[⚠️] Failed to execute script:", err) end end end) table.insert(sectionButtons[section], btnFrame) end function RightPanel.switchTo(section) for sec, buttons in pairs(sectionButtons) do for _, btn in ipairs(buttons) do btn.Visible = (sec == section) end end end return RightPanel end return RightPanel  end)() local __mod5 = (function() local COMPILE_SCRIPTS_OUTPUT = true scripts = { {id=1, section="Universal", name="Aimbot", description="Universal aimbot with FOV and tracer lines", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/aimbot.lua"}, {id=2, section="Universal", name="Infinite Yield", description="Credits to iy devs", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/iy.lua"}, {id=3, section="Universal", name="Fly GUI", description="A universal fly gui, doesnt work with body checking game (Eg games that check body velocity, hrp, so on, please use the Universal Powerful Fly script to bypass those types of game)", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/flyscript.lua"}, {id=4, section="Universal", name="Universal Fly Car", description="A universal fly car", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/flycar.lua"}, {id=5, section="Universal", name="Anti Death", description="Universal anti death script, anti death script always return your character body to its death position, something like a respawn point where your character dies!", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/antideath.lua"}, {id=6, section="Universal", name="Invisible tool", description="Allow yourself to be made invisible via the invisible tool!", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/invistool.lua"}, {id=7, section="Prison Life", name="Kill aura", description="Kill aura for Prison Life", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/prisonlife/killaura.lua"}, {id=8, section="Prison Life", name="Super Kill", description="Super Kill script for Prison Life", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/prisonlife/superkill.lua"}, {id=9, section="Universal", name="Tp Gui", description="TP gui, allow yourself to be tpied anywhere, with a tp tool! universal", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/tps/tpgui.lua"}, {id=10, section="Universal", name="TP Tool", description="A simple universal tp tool, allows your self to be tpied, if you wish to have gui please use the tp gui", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/tps/tptool.lua"}, {id=11, section="Universal", name="Waypoint", description="Universal Waypoint ", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/tps/waypoint.lua"} } local WindowControls = __mod2 local LeftPanel = __mod3 local RightPanel = __mod4 local Players = game:GetService("Players") local Main = {} function Main.create() local player = Players.LocalPlayer if not player then return end local gui = Instance.new("ScreenGui") gui.Name = "MainUI" gui.IgnoreGuiInset = true gui.ResetOnSpawn = false gui.Parent = player:WaitForChild("PlayerGui") local window = Instance.new("Frame") window.Size = UDim2.new(0, 700, 0, 450) window.Position = UDim2.new(0.05, 0, 0.05, 0) window.BackgroundColor3 = Color3.fromRGB(0, 0, 0) window.BackgroundTransparency = 0.4 window.Parent = gui local corner = Instance.new("UICorner", window) corner.CornerRadius = UDim.new(0, 16) local stroke = Instance.new("UIStroke", window) stroke.Color = Color3.fromRGB(200, 200, 200) stroke.Transparency = 0.5 stroke.Thickness = 2 local topBar = Instance.new("Frame") topBar.Size = UDim2.new(1, 0, 0, 35) topBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0) topBar.BackgroundTransparency = 0.3 topBar.Parent = window local content = WindowControls.create(topBar, window, gui) local rightPanel = RightPanel.create(content) local activeSection = nil local leftPanel leftPanel, _ = LeftPanel.create(content, function(sectionName) activeSection = sectionName rightPanel.switchTo(sectionName) end) local orderedSections, addedSections = {}, {} for _, data in ipairs(scripts) do if not addedSections[data.section] then table.insert(orderedSections, data.section) addedSections[data.section] = true end rightPanel.addButton(data.section, data) end for i, name in ipairs(orderedSections) do LeftPanel.addTab(name, i) end if #orderedSections > 0 then activeSection = orderedSections[1] rightPanel.switchTo(activeSection) end end Main.scripts = scripts return Main  end)() local __mod6 = (function() local __table6 = {} local function isGameReady() if typeof(game) ~= "Instance" or not game.GetService then return false end local Players = game:GetService("Players") if not Players then return false end if not Players.LocalPlayer then Players.PlayerAdded:Wait() end return true end local function createPersistentScreenGui(name) local Players = game:GetService("Players") local player = Players.LocalPlayer local playerGui = player:WaitForChild("PlayerGui") local existingGui = playerGui:FindFirstChild(name) if existingGui then return existingGui end local screenGui = Instance.new("ScreenGui") screenGui.Name = name screenGui.ResetOnSpawn = false screenGui.Parent = playerGui return screenGui end local function safeStart() if not isGameReady() then warn("[⚠️] Roblox environment not ready, waiting...") repeat task.wait(0.25) until isGameReady() end local gui = createPersistentScreenGui("MyMainUI") local Intro = __mod0 or {} if type(Intro.createIntro) == "function" then Intro.createIntro(function() local Main = __mod5 if type(Main.create) == "function" then Main.create(gui) else warn("[⚠️] create() not found in Main module") end end, gui) else local Main = __mod5 if type(Main.create) == "function" then Main.create(gui) else warn("[⚠️] create() not found in Main module!") end end end task.spawn(safeStart) return __table6 end)() return __mod6
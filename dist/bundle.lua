local __mod0 = (function() local Intro = {} function Intro.createIntro(callback) local Players = game:GetService("Players") local TweenService = game:GetService("TweenService") local RunService = game:GetService("RunService") local player = Players.LocalPlayer if not player then if callback then callback() end return end local playerGui = player:WaitForChild("PlayerGui") local gui = Instance.new("ScreenGui") gui.Name = "WhitzHubIntro" gui.Parent = playerGui gui.IgnoreGuiInset = true local bg = Instance.new("Frame") bg.Size = UDim2.new(1, 0, 1, 0) bg.BackgroundColor3 = Color3.fromRGB(0, 0, 0) bg.BackgroundTransparency = 1 bg.Parent = gui local title = Instance.new("TextLabel") title.Text = "WhitzHub" title.Size = UDim2.new(0, 400, 0, 80) title.Position = UDim2.new(0.5, 0, 0.4, 0) title.AnchorPoint = Vector2.new(0.5, 0.5) title.TextColor3 = Color3.new(1, 1, 1) title.TextScaled = true title.Font = Enum.Font.GothamBold title.BackgroundTransparency = 1 title.TextTransparency = 1 title.Parent = gui local subtitle = Instance.new("TextLabel") subtitle.Text = "Created by whitzscott" subtitle.Size = UDim2.new(0, 400, 0, 40) subtitle.Position = UDim2.new(0.5, 0, 0.55, 0) subtitle.AnchorPoint = Vector2.new(0.5, 0.5) subtitle.TextColor3 = Color3.fromRGB(200, 200, 200) subtitle.TextScaled = true subtitle.Font = Enum.Font.Gotham subtitle.BackgroundTransparency = 1 subtitle.TextTransparency = 1 subtitle.Position = subtitle.Position + UDim2.new(0, 0, 0.1, 0) subtitle.Parent = gui local tweenInfoIn = TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out) local tweenInfoOut = TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.In) local bgFadeIn = TweenService:Create(bg, tweenInfoIn, {BackgroundTransparency = 0.3}) local bgFadeOut = TweenService:Create(bg, tweenInfoOut, {BackgroundTransparency = 1}) local titleFadeIn = TweenService:Create(title, tweenInfoIn, {TextTransparency = 0}) local titleScale = TweenService:Create(title, TweenInfo.new(1, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 450, 0, 90)}) local titleFadeOut = TweenService:Create(title, tweenInfoOut, {TextTransparency = 1}) local titleShrink = TweenService:Create(title, TweenInfo.new(1, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0, 400, 0, 80)}) local subtitleFadeIn = TweenService:Create(subtitle, tweenInfoIn, {TextTransparency = 0, Position = UDim2.new(0.5, 0, 0.55, 0)}) local subtitleFadeOut = TweenService:Create(subtitle, tweenInfoOut, {TextTransparency = 1, Position = subtitle.Position + UDim2.new(0, 0, 0.1, 0)}) bgFadeIn:Play() titleFadeIn:Play() titleScale:Play() subtitleFadeIn:Play() titleFadeIn.Completed:Wait() task.wait(2) bgFadeOut:Play() titleFadeOut:Play() titleShrink:Play() subtitleFadeOut:Play() titleFadeOut.Completed:Wait() gui:Destroy() if callback and type(callback) == "function" then callback() end end return Intro  end)() local __mod1 = (function() local WindowControls = {} local UserInputService = game:GetService("UserInputService") local TweenService = game:GetService("TweenService") function WindowControls.create(topBar, window, gui) local buttonSize = UDim2.new(0, 30, 0, 30) local spacing = 5 local function createButton(index, imageId) local btn = Instance.new("ImageButton") btn.Size = buttonSize btn.Position = UDim2.new(1, -(index * (buttonSize.X.Offset + spacing)), 0, 2) btn.Image = imageId btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40) btn.BackgroundTransparency = 0.4 btn.BorderSizePixel = 0 local corner = Instance.new("UICorner", btn) corner.CornerRadius = UDim.new(0, 6) btn.Parent = topBar local hoverTween = TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 0.2}) local leaveTween = TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 0.4}) btn.MouseEnter:Connect(function() hoverTween:Play() end) btn.MouseLeave:Connect(function() leaveTween:Play() end) return btn end local closeButton = createButton(1, "rbxassetid://116533143721125") local minimizeButton = createButton(2, "rbxassetid://10137941941") local maximizeButton = createButton(3, "rbxassetid://11036884234") local isMinimized = false local isMaximized = false local originalSize = window.Size local originalPos = window.Position local contentFrame = Instance.new("Frame") contentFrame.Name = "ContentFrame" contentFrame.Size = UDim2.new(1, 0, 1, -35) contentFrame.Position = UDim2.new(0, 0, 0, 35) contentFrame.BackgroundTransparency = 1 contentFrame.ClipsDescendants = true contentFrame.Parent = window local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out) minimizeButton.MouseButton1Click:Connect(function() isMinimized = not isMinimized local goalSize = isMinimized and UDim2.new(originalSize.X.Scale, originalSize.X.Offset, 0, 35) or originalSize TweenService:Create(window, tweenInfo, {Size = goalSize}):Play() end) closeButton.MouseButton1Click:Connect(function() local flash = Instance.new("Frame") flash.Size = UDim2.new(1,0,1,0) flash.BackgroundColor3 = Color3.fromRGB(255,0,0) flash.BackgroundTransparency = 0.7 flash.BorderSizePixel = 0 flash.Parent = window TweenService:Create(flash, TweenInfo.new(0.15), {BackgroundTransparency = 1}):Play() local tween = TweenService:Create(window, TweenInfo.new(0.25), {Position = window.Position + UDim2.new(0,0,0,20), Size = UDim2.new(0,0,0,0)}) tween:Play() tween.Completed:Connect(function() gui:Destroy() end) end) maximizeButton.MouseButton1Click:Connect(function() isMaximized = not isMaximized local goal = isMaximized and {Size = UDim2.new(1,-20,1,-20), Position = UDim2.new(0,10,0,10)} or {Size = originalSize, Position = originalPos} TweenService:Create(window, tweenInfo, goal):Play() end) local dragging = false local dragInput, mousePos, framePos topBar.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true mousePos = input.Position framePos = window.Position input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end) end end) topBar.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end end) UserInputService.InputChanged:Connect(function(input) if input == dragInput and dragging then local delta = input.Position - mousePos window.Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y) end end) return contentFrame end return WindowControls  end)() local __mod2 = (function() local TweenService = game:GetService("TweenService") local LeftPanel = {} function LeftPanel.create(parent, onTabClick) local frame = Instance.new("Frame") frame.Name = "LeftPanel" frame.Size = UDim2.new(0, 150, 1, 0) frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) frame.BackgroundTransparency = 0.3 frame.Parent = parent local corner = Instance.new("UICorner", frame) corner.CornerRadius = UDim.new(0, 8) local scroll = Instance.new("ScrollingFrame") scroll.Size = UDim2.new(1, 0, 1, 0) scroll.BackgroundTransparency = 1 scroll.ScrollBarThickness = 6 scroll.Parent = frame local layout = Instance.new("UIListLayout", scroll) layout.SortOrder = Enum.SortOrder.LayoutOrder layout.Padding = UDim.new(0, 5) layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function() scroll.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 5) end) function LeftPanel.addTab(name, order) local btn = Instance.new("TextButton") btn.Size = UDim2.new(1, -10, 0, 40) btn.Text = name btn.TextColor3 = Color3.fromRGB(255, 255, 255) btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70) btn.LayoutOrder = order btn.Parent = scroll local corner = Instance.new("UICorner", btn) corner.CornerRadius = UDim.new(0, 6) btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(100, 100, 100)}):Play() end) btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(70, 70, 70)}):Play() end) btn.MouseButton1Click:Connect(function() if onTabClick then onTabClick(name) end end) end return LeftPanel, frame end return LeftPanel  end)() local __mod3 = (function() local TweenService = game:GetService("TweenService") local UserInputService = game:GetService("UserInputService") local RightPanel = {} function RightPanel.create(parent) local frame = Instance.new("Frame") frame.Name = "RightPanel" frame.Size = UDim2.new(1, -160, 1, 0) frame.Position = UDim2.new(0, 160, 0, 0) frame.BackgroundTransparency = 1 frame.Parent = parent local layout = Instance.new("UIListLayout", frame) layout.SortOrder = Enum.SortOrder.LayoutOrder layout.Padding = UDim.new(0, 5) local tooltip = Instance.new("TextLabel") tooltip.Name = "Tooltip" tooltip.BackgroundColor3 = Color3.fromRGB(50, 50, 50) tooltip.BackgroundTransparency = 0.3 tooltip.TextColor3 = Color3.fromRGB(255, 255, 255) tooltip.TextSize = 16 tooltip.RichText = true tooltip.TextWrapped = true tooltip.TextXAlignment = Enum.TextXAlignment.Left tooltip.TextYAlignment = Enum.TextYAlignment.Top tooltip.Visible = false tooltip.Parent = parent.Parent local corner = Instance.new("UICorner", tooltip) corner.CornerRadius = UDim.new(0, 8) local stroke = Instance.new("UIStroke", tooltip) stroke.Color = Color3.fromRGB(200,200,200) stroke.Transparency = 0.5 stroke.Thickness = 2 local sectionButtons = {} function RightPanel.addButton(section, data) if not sectionButtons[section] then sectionButtons[section] = {} end local btn = Instance.new("TextButton") btn.Size = UDim2.new(1, -10, 0, 40) btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60) btn.TextColor3 = Color3.new(1, 1, 1) btn.Text = data.name btn.Visible = false btn.LayoutOrder = data.id btn.Parent = frame local corner = Instance.new("UICorner", btn) corner.CornerRadius = UDim.new(0, 6) btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(85,85,85)}):Play() tooltip.Text = data.description or "" tooltip.Size = UDim2.new(0, 250, 0, math.clamp(tooltip.TextBounds.Y + 8, 40, 200)) local mousePos = UserInputService:GetMouseLocation() local screenSize = workspace.CurrentCamera.ViewportSize tooltip.Position = UDim2.new(0, math.clamp(mousePos.X + 10, 0, screenSize.X - tooltip.Size.X.Offset), 0, math.clamp(mousePos.Y + 10, 0, screenSize.Y - tooltip.Size.Y.Offset)) tooltip.Visible = true end) btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play() tooltip.Visible = false end) btn.MouseButton1Click:Connect(function() if data.url and #data.url > 0 then local success, err = pcall(function() local func, loadErr = loadstring(game:HttpGet(data.url, true)) if not func then error(loadErr) end func() end) if not success then warn("[⚠️] Failed to execute script:", err) end end end) table.insert(sectionButtons[section], btn) end function RightPanel.switchTo(section) for sec, buttons in pairs(sectionButtons) do for _, btn in ipairs(buttons) do btn.Visible = (sec == section) end end end return RightPanel end return RightPanel  end)() local __mod4 = (function() local COMPILE_SCRIPTS_OUTPUT = true scripts = { {id=1, section="Universal", name="Aimbot", description="Universal aimbot with FOV and tracer lines", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/aimbot.lua"}, {id=2, section="Universal", name="Infinite Yield", description="Credits to iy devs", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/iy.lua"}, {id=3, section="Universal", name="Fly GUI", description="A universal fly gui, doesnt work with body checking game (Eg games that check body velocity, hrp, so on, please use the Universal Powerful Fly script to bypass those types of game)", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/flyscript.lua"}, {id=4, section="Universal", name="Anti Death", description="Universal anti death script, anti death script always return your character body to its death position, something like a respawn point where your character dies!", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/antideath.lua"}, {id=5, section="Universal", name="Invisible tool", description="Allow yourself to be made invisible via the invisible tool!", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/invistool.lua"}, {id=6, section="Prison Life", name="Kill aura", description="Kill aura for Prison Life", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/prisonlife/killaura.lua"}, {id=7, section="Prison Life", name="Super Kill", description="Super Kill script for Prison Life", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/prisonlife/superkill.lua"}, {id=8, section="Universal", name="Tp Gui", description="TP gui, allow yourself to be tpied anywhere, with a tp tool! universal", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/tps/tpgui.lua"}, {id=9, section="Universal", name="TP Tool", description="A simple universal tp tool, allows your self to be tpied, if you wish to have gui please use the tp gui", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/tps/tptool.lua"}, {id=10, section="Universal", name="Waypoint", description="Universal Waypoint ", url="https://raw.githubusercontent.com/wolf-whitz/WhitzHub/main/src/scripts/luas/universal/tps/waypoint.lua"} } local WindowControls = __mod1 local LeftPanel = __mod2 local RightPanel = __mod3 local Players = game:GetService("Players") local Main = {} function Main.create() local player = Players.LocalPlayer if not player then return end local gui = Instance.new("ScreenGui") gui.Name = "MainUI" gui.IgnoreGuiInset = true gui.ResetOnSpawn = false gui.Parent = player:WaitForChild("PlayerGui") local window = Instance.new("Frame") window.Size = UDim2.new(0, 700, 0, 450) window.Position = UDim2.new(0.05, 0, 0.05, 0) window.BackgroundColor3 = Color3.fromRGB(0, 0, 0) window.BackgroundTransparency = 0.4 window.Parent = gui local corner = Instance.new("UICorner", window) corner.CornerRadius = UDim.new(0, 16) local stroke = Instance.new("UIStroke", window) stroke.Color = Color3.fromRGB(200, 200, 200) stroke.Transparency = 0.5 stroke.Thickness = 2 local topBar = Instance.new("Frame") topBar.Size = UDim2.new(1, 0, 0, 35) topBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0) topBar.BackgroundTransparency = 0.3 topBar.Parent = window local content = WindowControls.create(topBar, window, gui) local rightPanel = RightPanel.create(content) local activeSection = nil local leftPanel leftPanel, _ = LeftPanel.create(content, function(sectionName) activeSection = sectionName rightPanel.switchTo(sectionName) end) local orderedSections, addedSections = {}, {} for _, data in ipairs(scripts) do if not addedSections[data.section] then table.insert(orderedSections, data.section) addedSections[data.section] = true end rightPanel.addButton(data.section, data) end for i, name in ipairs(orderedSections) do LeftPanel.addTab(name, i) end if #orderedSections > 0 then activeSection = orderedSections[1] rightPanel.switchTo(activeSection) end end Main.scripts = scripts return Main  end)() local __mod5 = (function() local __table5 = {} local function isGameReady() if typeof(game) ~= "Instance" or not game.GetService then return false end local Players = game:GetService("Players") if not Players then return false end if not Players.LocalPlayer then Players.PlayerAdded:Wait() end return true end local function safeStart() if not isGameReady() then warn("[⚠️] Roblox environment not ready, waiting...") repeat task.wait(0.25) until isGameReady() end local Intro = __mod0 or {} if type(Intro.createIntro) == "function" then Intro.createIntro(function() local Main = __mod4 if type(Main.create) == "function" then Main.create() else warn("[⚠️] create() not found in Main module") end end) else local Main = __mod4 if type(Main.create) == "function" then Main.create() else warn("[⚠️] create() not found in Main module") end end end task.spawn(safeStart) return __table5 end)() return __mod5